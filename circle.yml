machine:
  timezone:
    America/New_York
  php:
    version: 7.0.11
  node:
    version: v4.1.2

dependencies:
  cache_directories:
    - node_modules
  override:
    - npm install

test:
  override:
    - gulp css