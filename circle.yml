machine:
  timezone:
    America/New_York
  php:
    version: 7.0.11
  node:
    version: 6

dependencies:
  cache_directories:
    - node_modules
  override:
    - yarn install
    - node_modules/.bin/gulp install
    - npm install

test:
  override:
    - gulp css